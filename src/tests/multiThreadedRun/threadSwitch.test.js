import * as actions from 'actions/codeWalkActions'
import * as types from 'store/types'
import {state} from './multiThreadedDummyState';


describe('Thread switch in codewalk', () => {
  it('should update active timestamp on increment', () => {

  })
  
  it('takes to the thread position nearest to the active timestamp during thread switch - 1', () => {

  })

  it('takes to the thread position nearest to the active timestamp during thread switch - 2', () => {

  })

})

