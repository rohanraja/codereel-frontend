import initialState from '../../store/initialState'
import * as types from './types'
// -- import_hook --

export function Code_headerReducer(state = initialState.activeFileName, action) {
  switch (action.type) {

    default:
      return state
  }
}

export function codeReducer(state = initialState.codeFiles, action) {
  switch (action.type) {

    default:
      return state
  }
}

// -- newmethod_hook --
